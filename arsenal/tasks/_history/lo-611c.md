---
id: lo-611c
title: "R-0: merge includeMetadata + includeTimestamps into showMetaInfo (touches popup — hold until popup work settles)"
priority: 5
deps: ["lo-f061"]
workspace: "EXPORTERS"
tags: ["redesign", "popup"]
status: merged
pr: https://github.com/nuncaeslupus/ai-chat-exporter/pull/178
---

**HOLD.** Touches `src/extension/popup/` while the popup redesign is being
worked in another session. Do not claim this until that settles — it is the one
task in the redesign set with a real collision. A pre-existing branch
`arsenal/lo-f096-export-options-ui-metadata-and-timestamp` covers the same rows.

Full step-by-step plan already written:
`docs/superpowers/plans/2026-07-29-exporters-redesign-phase-0.md`, Task 2.

Summary: two checkboxes governed one idea, and one of them governed nothing —
no parser wrote `Message.timestamp`, so `includeTimestamps` rendered nothing in
any format. Collapse to one `showMetaInfo` (default **true**) gating the header
block, the per-turn times and the day separators together. Includes the storage
migration and all seven locale strings (translations are in the plan).

## Acceptance gate

```bash
pnpm test:run tests/unit/core/exporters/show-meta-info.test.ts tests/unit/extension/popup/popup-options.test.ts && test -z "$(grep -rl 'optionIncludeTimestamps' _locales src)"
```

## Tests
New `tests/unit/core/exporters/show-meta-info.test.ts`, plus the ~20 existing
files that set the old fields.

## Location
`src/core/types/{exporter,config}.ts`, `src/shared/{constants,messages}.ts`,
six exporters, `src/extension/{content,popup}/`, `_locales/*/messages.json`.
