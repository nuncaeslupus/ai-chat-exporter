---
id: lo-3e45
title: "R10: brand assets — official Claude logo + final extension logo choice (human)"
priority: 5
workspace: "POPUP"
tags: ["redesign", "human"]
status: merged
pr: https://github.com/nuncaeslupus/ai-chat-exporter/pull/106
---

**Blocked on a human — do not dispatch a worker.** Both items are brand
decisions or trademark downloads an agent should not make.

## Acceptance gate

**Gate**: `src/assets/icons/claude-logo.svg` is the official Anthropic Claude
mark, and the extension's own logo is the author's final choice.

## What is needed

1. **Claude logo.** The repo currently ships only the Anthropic "A"
   (`src/assets/icons/claude-logo.svg`); the mocks show a hatched placeholder.
   Download the official mark from Anthropic's brand kit and save it under that
   same filename. Check the brand guidelines permit the use — this is a
   third-party trademark, which is why it is not an agent's call.
2. **Extension logo.** Three candidates exist in the design document (A solid,
   B outline, D three-dot). The mocks use **A**; the author has not made the
   final call. Once chosen, regenerate `icon-16/32/48/128.png`.
3. Optional, same class: a colour ChatGPT mark instead of today's black outline.

`src/assets/icons/gemini-spark.svg` is already committed — it was derived from
the repo's own `gemini-logo.svg` path, so it needed no external asset.

## Location

`src/assets/icons/`, `manifest` icon entries.
