# Payload: lo-0906 — Source zip ships agent tooling to reviewers

## Acceptance gate

**Gate**: `dist/ai-chat-exporter-source.zip` contains only what is needed to reproduce the build.


Prose-only gate — verified by worker judgment, no script to run.

`package.json:26` `package:source` excludes `node_modules`, `dist`, `.git`, logs, editor dirs, `tmp`, `coverage` — but not `.claude/` (1.1 MB, 115 files), `.agents/`, or `claude-arsenal/`. Firefox reviewers use this zip to reproduce the build from source; it currently hands them a pile of AI agent configuration that has nothing to do with the product.

Also unresolved: `docs/dev/releasing.md:68,88` tells reviewers a `BUILD_INSTRUCTIONS.md` ships in this zip. That file does not exist anywhere in the repo. Firefox source review fails without reproducible build instructions — this is a real submission blocker, and the same doc's troubleshooting section already lists "Firefox source validation fails" as its first entry.
