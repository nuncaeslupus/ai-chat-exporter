# Payload: lo-2420 — Popup a11y and hardcoded strings

## Acceptance gate

**Gate**: status changes are announced to screen readers; no user-visible English literal bypasses `getMessage()`.


Prose-only gate — verified by worker judgment, no script to run.

- `src/extension/popup/popup.html:17` — status text updates in place with no `aria-live` region, so a screen-reader user gets no notification when the state changes from "Ready" to "Exporting" to an error. Given that the status line is the only feedback channel the extension has, this makes failure states completely invisible to those users.
- `src/extension/popup/popup.ts:148` and nearby — several user-visible English strings are hardcoded instead of going through the i18n system the same file uses elsewhere. Grep for string literals reaching `textContent`/`innerText`.

Same pass as `lo-02cf` (contrast) and `lo-934c` (dark mode).
