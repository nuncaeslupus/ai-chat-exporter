# Payload: lo-45b2 — Artifacts appear twice in every export

## Acceptance gate

**Gate**: an artifact appears exactly once per export.


Prose-only gate — verified by worker judgment, no script to run.

`src/core/services/conversation-structure-service.ts:48` — artifacts are emitted both as the plain-text marker baked into `message.content` and again as the full metadata section the exporters render. Every format shows both.

Fix alongside `lo-3005` — same function, and the correct design (parse htmlContent, append markers as blocks) determines which copy survives here.
