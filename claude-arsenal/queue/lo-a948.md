# P-1: popup needs more horizontal breathing room

Author feedback, 2026-07-30: "the popup could have some more internal left and
right padding (do it slightly bigger if you need it)".

**Current values** (`src/extension/popup/popup.css`):

- `--popup-width: 420px`
- header `.popup-header` padding `14px 18px`
- `.setting-rows` padding `14px 18px 0`
- `.conversation-block` / body areas padding `16px 18px 0`

So the horizontal inset is **18px** throughout. The ask is more.

**Explicitly allowed:** grow `--popup-width`. Chrome's ceiling is 800px and the
popup is 420px, so there is plenty of room. Widening is preferable to squeezing
the content column.

**Do it once, at the token.** The 18px appears in several rules; introduce a
`--pad-x` custom property and use it everywhere rather than editing each rule,
or the next change repeats this hunt.

**Watch:** the fixed body height (`--body-height`) and the "popup never changes
size" invariant from the design. Widening is fine; the box must still not jump
between states.

## Acceptance gate

The popup renders at the new width with no horizontal scrollbar and no state
that changes height.

```bash
pnpm test:run tests/unit/extension/popup/
```

## Tests
`tests/unit/extension/popup/popup-router.test.ts` covers the fixed body box.
Add nothing unless a value is asserted; this is a visual change.

## Location
`src/extension/popup/popup.css`

---

## ORCHESTRATOR NOTE 2026-07-30 — one PR covers P-1, P-2 and P-3

P-1 (`lo-a948`), P-2 (`lo-737a`) and P-3 (`lo-4d34`) are a single change to the
popup's horizontal rhythm and scroll chrome in `popup.css`. They are dispatched
to **one worker as one PR**, per P-2's own "best done together" note — three
separate PRs would each re-measure the same padding and conflict in the same
file.

Deliver all three in this branch:

1. **P-1** — introduce a `--pad-x` token and raise the 18px inset everywhere it
   appears (header, `.setting-rows`, `.conversation-block`). Growing
   `--popup-width` past 420px is explicitly allowed (Chrome's ceiling is 800px).
2. **P-2** — give the chevron its own right inset so every navigable row
   (Contenido, Opciones, Nombre del archivo) has visible space between arrow and
   row edge, and they all agree.
3. **P-3** — slim integrated scrollbar via `scrollbar-width: thin` +
   `scrollbar-color`, plus `::-webkit-scrollbar` for older Chrome, on **every**
   scrolling area (format menu, pair chooser, options list). Colours must come
   from existing surface/muted **tokens** — a hardcoded grey that fails dark mode
   is caught by the contrast test.

Invariant across all three: the box must not change height between states.

Run all three gates before opening the PR:

```bash
pnpm test:run tests/unit/extension/popup/ tests/unit/accessibility/contrast.test.ts
```
