# Payload: lo-934c — No dark mode

## Acceptance gate

**Gate**: the popup and exported HTML respect `prefers-color-scheme: dark`.


Prose-only gate — verified by worker judgment, no script to run.

`src/core/exporters/html-exporter.ts:306` and the popup CSS have no `prefers-color-scheme` handling — the popup is a bright panel on a dark browser, and every exported document is white regardless.

Lowest priority of the UI group, but it is the same files as `lo-02cf` and `lo-2420`; folding it into one visual pass costs almost nothing extra.

## Update (redesign)

Blocked on **`lo-c39f` (R1)**, which introduces the design-token custom
properties. Once those exist this task is a token swap plus the exported-HTML
half — the dark values are tabulated in `docs/design/popup-redesign.md` § 7
(`prefers-color-scheme: dark`): background `#141E1B`, header `#0B1F1A`, surfaces
`#1C2926` / `#182421`, bottom bar `#101917`, borders `#293833` / `#2E3E38`, text
`#E9F0ED`, secondary `#8FA29C` / `#7C8C87`, accent `#3FB394` on `#07211A`, tint
`#1E332D`, light accent `#6FD3B5`.

If R1 left any hex value inline in a component rule instead of in the token
table, fix that here rather than adding a second dark override.
