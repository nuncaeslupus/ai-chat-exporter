# Payload: lo-5e70 — Stale dev docs

**Gate**: every command in `docs/dev/**` runs, and every file referenced exists.

- `docs/dev/development-plan.md:33` — lists `md-exporter.ts` as a Key File; that is the dead one (`lo-ce4b`), the live one is `structured-md-exporter.ts`.
- `:41` — "Print Functionality" listed as a High Priority next step; print shipped in v1.1.1.
- `:44` — "Claude.ai & Gemini Parsers" grouped as pending; Claude is done.
- `docs/dev/testing-guide.md:195,207` — `pnpm test:watch` and `pnpm test:ui` do not exist in `package.json`.
- `docs/dev/testing-guide.md:253-254` — example instantiates `new SelectionService()`; the class is entirely static.
- `docs/dev/README.md:36,41` — reference `.agents/*/SKILL.md` uppercase; the files are lowercase `skill.md`. Breaks on case-sensitive filesystems.
- `docs/dev/project-structure.md:42` — says `build/` is gitignored. No longer true as of PR #4, and the claim is what caused the vite configs to go untracked in the first place.
- `docs/dev/releasing.md:68,88` — `BUILD_INSTRUCTIONS.md` does not exist (also tracked in `lo-0906`).
- No dev doc mentions the `Makefile` added in PR #4; they all still say `pnpm`.

## Additional drift found 2026-07-28 (by the lo-519c worker)

`docs/dev/building.md:26` and `docs/dev/workflow.md:87` document `pnpm dev` as
building "Both browsers". It never did — the script was simply broken (no
`--config`). `lo-519c` (PR #20) aliased `dev` to `dev:chrome`, so it now builds
Chrome only. Either correct both docs to say Chrome, or add a real both-browsers
script (which needs a `concurrently` / `npm-run-all` dependency — a deliberate
call, not a doc fix).
