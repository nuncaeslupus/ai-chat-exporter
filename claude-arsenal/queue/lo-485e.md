# Payload: lo-485e — Register GeminiParser

**Gate**: on a live gemini.google.com conversation the console shows `[AI Chat Exporter] Detected platform: Gemini` followed by a non-zero pair count.

## Work

`src/core/parsers/index.ts:25` — the registry row is commented out with `// TODO: Uncomment when parser is implemented`. Uncomment it and confirm `createParserForDocument` has the matching `gemini` switch branch.

## References

- `src/core/parsers/index.ts:22-27` — `parserRegistry`
- `src/extension/content/content-script.ts:26-38` — the `detectParser()` call whose log line is the gate

## Context

Verification is live-browser only (needs a logged-in Google account): `make build`, reload the unpacked extension at `chrome://extensions`, open a Gemini conversation, read the console. Blocked on lo-194d (manifest) and lo-3920 (fixtures prove the selectors before touching the registry).
