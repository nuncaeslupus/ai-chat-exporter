# Payload: lo-f096 — Export Options UI

**Gate**: toggling an option in the popup changes the exported output and the choice survives a browser restart.

## Tests

- `storage round-trips export options`
- `exporter omits metadata block when the metadata option is off`

## References

- `src/extension/popup/popup.ts`, `src/extension/popup/popup.html` — the popup surface
- `src/shared/storage.ts` — `StorageService`, already used for persisted state
- `docs/dev/development-plan.md` — "Export Options UI" is High Priority #1

## Context

Scope: metadata block, timestamps. Do not build a settings page — the popup already has the room.
