# Payload: lo-8d36 — Tests for pdf, docx and html exporters

## Acceptance gate

**Gate**: `pnpm test:run` green with new suites for all three exporters; every exporter under `src/core/exporters/` has at least one test file.


Prose-only gate — verified by worker judgment, no script to run.

## Tests

- `html-exporter` — emits a full document, escapes user content, embeds highlight.js classes
- `docx-exporter` — produces a non-empty Buffer/Blob; headings and code blocks map to the expected `docx` elements
- `pdf-exporter` — assert on the structured input handed to jsPDF rather than the rendered bytes

## References

- `tests/unit/core/exporters/md-exporter.test.ts` — the fixture + assertion pattern to copy
- `src/core/exporters/{html,docx,pdf}-exporter.ts` — the three untested exporters
- `tests/setup/` — existing jsdom setup

## Context

`pdf-exporter` uses jsPDF + html2canvas, which does not run under jsdom — mock the jsPDF surface and assert the calls. These three are exactly the formats that break silently on a bad export, which is why they are worth the mock effort.
