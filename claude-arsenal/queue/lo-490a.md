# Payload: lo-490a — Make the exporter generator a real skill

## Acceptance gate

**Gate**: asking for a new export format loads the skill; its scaffolding compiles against `base-exporter.ts`.


Prose-only gate — verified by worker judgment, no script to run.

Same situation as `lo-b617`: `.agents/exporter-generator/skill.md` (498 lines) + `prompts/analyze-format.md` (307) + `prompts/generate-exporter.md` (369) exist, are never loaded (wrong directory, no frontmatter), and teach an `export() => Promise<Blob>` signature the code does not have — the real one is `export(conversation, selectedPairs, options) => Promise<ExportResult>`.

Port to `.claude/skills/exporter-generator/`, rewrite against the real base class and registry (`Map<ExportFormat, ExporterFactory>`), and have it emit a test file alongside the exporter — the three newest exporters shipped without tests, which is exactly what a generator skill should prevent.

Depends on `lo-58b0`.
