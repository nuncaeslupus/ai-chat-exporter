# Payload: lo-3920 — Gemini fixtures + parser tests

**Gate**: `pnpm test:run` green with a new `tests/unit/core/parsers/gemini.test.ts` asserting against a real DOM capture.

## Tests

- `canParse() returns true for a gemini.google.com document` — URL + `conversation-ui` container present
- `getTitle() extracts the conversation title` — falls back to `'Gemini Conversation'`
- `extractQAPairs() returns one pair per conversation turn`
- `pairs have user question and assistant answer` — mirrors the Claude suite
- `assistant htmlContent preserves code blocks`

## References

- `tests/unit/core/parsers/claude.test.ts` — the pattern to copy verbatim (fixture load + jsdom + assertions)
- `tests/fixtures/dom-snapshots/claude/real-capture.html` — capture format and size expectation
- `tests/fixtures/dom-snapshots/gemini/` — exists, empty; the capture goes here as `real-capture.html`
- `src/core/parsers/gemini/parser.ts` — uncommitted WIP already encoding real selectors

## Context

Capture the DOM from a live Gemini conversation via the browser tools (`document.documentElement.outerHTML`) — a logged-in Google session is needed, so the capture step runs in the user's Chrome, not headless. Include a conversation with a code block, a list, and at least one artifact chip so lo-1316 can reuse the same fixture.
