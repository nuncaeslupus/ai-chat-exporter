# Payload: lo-1316 — Gemini artifacts and thinking

**Gate**: fixture-backed tests assert each `immersive-entry-chip` maps to the right artifact type and that `thinking-content` is captured when present.

## Tests

- `extractArtifacts maps chip icons to artifact types` — one case per `GEMINI_ARTIFACT_ICONS` key present in the fixture
- `extractArtifacts falls back to type unknown for an unmapped icon`
- `extractThinking returns the reasoning text when the panel is expanded`

## References

- `src/core/parsers/gemini/parser.ts` — `extractArtifacts()`, `extractThinking()`, `extractImages()`
- `src/core/parsers/gemini/selectors.ts` — `GEMINI_ARTIFACT_ICONS` map (icon name → type/label)
- `src/core/parsers/chatgpt/parser.ts` — existing artifact handling to stay consistent with

## Context

Open decision: chips currently record only that an artifact exists — its content lives in the canvas panel and requires a click to load. Either keep title-only (cheap, honest) or drive the panel open before parsing. Pick one and note it in the parser; do not leave it ambiguous.
