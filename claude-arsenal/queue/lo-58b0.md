# Payload: lo-58b0 — The "how to add X" guides teach a nonexistent API

**Gate**: a contributor following each guide verbatim produces code that compiles against the current base classes.

## Confirmed drift in `docs/dev/adding-parsers.md`

| Doc says | Code actually has |
|---|---|
| `platformInfo.platform` | `platformInfo.id` (`base-parser.ts`) |
| `new ClaudeParser()` | constructor requires a `Document` (`parsers/index.ts:24`) |
| `protected extractMessages(): Message[]` | `protected extractQAPairs(config: ParserConfig): QAPair[]` |
| `canParse()` reads `window.location.href` | uses `this.getUrl()` |
| `const PARSERS: IParser[]` array + `getParserByPlatform()` | `Map<Platform, ParserFactory>` + `getParser`/`detectParser`/`createParserForDocument` |

## Confirmed drift in `docs/dev/adding-exporters.md`

- Doc: `export(conversation, options?) => Promise<Blob>`. Real (`base-exporter.ts:26-30`): `export(conversation, selectedPairs, options) => Promise<ExportResult>`.
- Doc: `EXPORTERS: Record<ExportFormat, typeof BaseExporter>`. Real: `Map<ExportFormat, ExporterFactory>`.

## Context

Blocks `lo-b617` and `lo-490a` — the generator skills are built on these guides, so the guides must be true before the skills encode them.
