# Payload: port the harness fixes upstream, or lose them

**Blocked on a human — the upstream repo is not reachable from a worker.**

## Acceptance gate

**Gate**: the two fixes below exist in the upstream claude-arsenal plugin, so the
next `/init` run does not revert them.

## Why this exists

`claude-arsenal/bin/` is vendored bundle code. `/init` refreshes any file whose
checksum differs from the plugin source — so a fix made only in this repo is
**silently reverted** the next time `/init` runs, and the false-`done` holes
reopen without anyone noticing.

Two fixes landed here tonight and both are at risk:

1. **PR #123 (`lo-19c0` / D-14)** — `gate_run.sh`: a gate that *cannot run*
   (exit 126/127) now exits **3** instead of reading as a pass; the reason prints
   to stdout as well as stderr so a `| tail` caller cannot swallow it; the
   package-manager shim is whitelisted onto the hardened PATH (with
   `COREPACK_HOME` pointed at the real cache, since a throwaway `HOME` made
   corepack re-download pnpm on every run); and a missing payload is resolved from
   the coordination ref into a temp file outside the repo.
2. **PR #125 (`lo-fabc` / D-16)** — `release.sh`: dropped the `-f payload` guard
   that skipped the gate entirely (measured: **14 of 20** `done` releases in the
   2026-07-28/29 session recorded with no gate run), and every `gate_run.sh` exit
   is now handled explicitly — 1 refuses, 3 refuses as "could not run", 2 allows
   but prints `Recording done UNVERIFIED`, and a missing `gate_run.sh` refuses
   instead of silently skipping.

Tests came with them: `tests/gate_run_test.sh` (9 checks) and
`tests/release_gate_test.sh` (15 checks). Port those too — they are what make the
fixes stick.

## Also worth carrying upstream

The `worker.md` protocol should tell workers they no longer need to materialize a
payload to run their gate — `gate_run.sh` resolves it from `arsenal-queue` now.
Three workers hit that trap in one session and each solved it by hand.

## Unrelated tooling note found the same night

`gh pr edit` fails in this repo with a `projects (classic) deprecated` GraphQL
error. Use `gh api repos/{owner}/{repo}/pulls/{n} -X PATCH -F body=@file` instead.


---

## Findings, 2026-07-29 (orchestrator)

**The urgent half of this task is already closed.** The reason it was p7 was the
silent-revert risk: `/init` refreshes any bundle file whose checksum differs from
the plugin source, so a fix living only in `claude-arsenal/bin/` would be
reverted. Checked directly:

| File | Vendored asset vs live | Verdict |
| --- | --- | --- |
| `claude-arsenal/bin/gate_run.sh` | identical | safe from `/init` |
| `claude-arsenal/bin/release.sh` | identical | safe from `/init` |
| `claude-arsenal/agents/worker.md` (payload note) | present in the asset | safe |
| `tests/gate_run_test.sh` | **not vendored** | `/init` does not manage it, so nothing to revert |
| `tests/release_gate_test.sh` | **not vendored** | same |

So nothing in this repo will regress. What remains is the genuine upstream
contribution, so other consumers get the fixes and a future re-vendor cannot
reintroduce the holes.

## Prepared and ready to send

A submission bundle was generated (session scratchpad, `upstream/`):

- `01-gate_run-cannot-run-exits-3.patch` — the PR #123 diff, harness files only
- `02-release-always-runs-the-gate.patch` — the PR #125 diff, harness files only
- current full copies of all four files, since upstream's layout differs from the
  vendored one
- `PR-BODY.md` — the write-up, with the measured `14 of 20` figure

**The one missing input is the upstream repo URL.** It is not discoverable
locally: `~/.claude/plugins/config.json` has `{"repositories": {}}`, the plugin
cache at `~/.claude/plugins/cache/claude-arsenal/` is not a git repo and is stale
(June 2, and it holds only `core/` and `skill-creator/` — no `init/assets`), and
`check_update.sh` reports no `arsenal` remote. Ask the owner for the marketplace
URL, then this is a branch, four files and a PR body away.


---

## RESOLVED, 2026-07-30 — the port already happened upstream

The upstream repo turned out to be `nuncaeslupus/claude-arsenal` (private, and
therefore invisible to every discovery path tried earlier). Cloned and compared
file by file against this repo. **Everything this task asked for is already
there**, landed as upstream `bb83092` — *"fix(core): close the two ways an
acceptance gate could pass without running (#133)"* — with `b886b2d` (#134)
adding the docs on how vendored updates silently fail.

| File | Upstream path | Verdict |
| --- | --- | --- |
| `gate_run.sh` | `plugins/core/skills/init/assets/bin/` | **identical** |
| `release.sh` | `plugins/core/skills/init/assets/bin/` | **identical** |
| `worker.md` (payload note) | `plugins/core/skills/init/assets/agents/` | **identical** |
| `gate_run_test.sh` | `plugins/core/tests/` | upstream is a **superset** — 18 gates vs our 9 |
| `release_gate_test.sh` | `plugins/core/tests/` | upstream is a **superset** |

The two test files differ only because upstream's are further developed, not
because anything is missing. Both local-only assertions were checked
individually and both exist upstream in a stronger form:

- *"the unrunnable gate announces itself on stdout"* — upstream asserts it on
  **both** stdout and stderr (its Gate 14).
- *"the coordination-ref fallback must not write into the repo tree"* — upstream's
  Gate 17 builds a real throwaway repo with a real `arsenal-queue` branch and
  asserts the payload is never materialized. Our local version **skips itself**
  when the payload happens to be on disk, so upstream's is the more reliable test.

No PR is needed. The prepared patch bundle is therefore unnecessary and was not
submitted — submitting it would have duplicated work already merged.

**Optional follow-up, not required by this task:** our local `tests/*_test.sh`
are the older, smaller copies. They are host-repo tests, not vendored, so nothing
reverts them and nothing is broken — but adopting upstream's stronger versions
would be an improvement if anyone wants it.
