# Payload: lo-6b97 — D-1: Stale `ivansaul/ai-chat-exporter` links in store listings

## Acceptance gate

**Gate**: no `github.com/ivansaul/` URL remains anywhere in `docs/store-listings/`; every remaining GitHub link resolves.


Prose-only gate — verified by worker judgment, no script to run.

## The divergence

The repo's actual owner is `nuncaeslupus` (`git remote -v`). The store listings
still point at `ivansaul` for issues, source, and license.

`lo-72f1` (PR #9) fixed **only** the PRIVACY.md link across the four listings.
Every other stale link was explicitly scoped out and is still there — found by
the lo-72f1 worker:

- `docs/store-listings/chrome-web-store-v1.1.1.txt:133`
- `docs/store-listings/firefox-addons-v1.1.1.txt:157,160,203`
- plus whatever the v1.1.0 archives carry (check, but they are historical —
  decide deliberately whether to touch them).

## Work

`grep -rn 'ivansaul' docs/` and correct each live link. Verify each corrected
URL actually resolves before claiming done.

## Context

Adjacent to `lo-5cda`, which covers stale claims in `README.md` / `docs/usage.md`
but **not** `docs/store-listings/`. Do not duplicate that task's scope.
