# Payload: lo-4e26 — Unblock `pnpm validate`

**Gate**: `make validate` exits 0 (lint + typecheck + 194 existing tests).

## Work

1. `eslint.config.js:1-2` imports `@eslint/js` and `typescript-eslint`; neither is in `package.json` devDependencies, so `pnpm lint` dies with `ERR_MODULE_NOT_FOUND` on a clean install. Add both, matching the installed eslint 9.x line.
2. `src/extension/content/content-script.ts:344` passes a `highlight` option to `marked`. `marked` v17 removed it — 3 `tsc` errors (TS2353 + two TS7006). Port to the `marked-highlight` extension or a custom renderer, keeping the highlight.js behaviour identical.

## References

- `package.json` — devDependencies block; `marked: ^17.0.1`, `highlight.js: ^11.11.1`
- `src/extension/content/content-script.ts:340-350` — the failing `marked.setOptions` call

## Context

Every other queued task's gate runs `make validate`, so this lands first. Tests are already green (194 passing) — do not modify them.
