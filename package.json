{
  "name": "ai-chat-exporter",
  "version": "1.2.1",
  "description": "Browser extension to export AI chatbot conversations to PDF, Markdown, DOCX, and more",
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --config build/vite.chrome.ts --mode development",
    "dev:chrome": "vite build --watch --config build/vite.chrome.ts --mode development",
    "dev:firefox": "vite build --watch --config build/vite.firefox.ts --mode development",
    "build": "pnpm build:chrome && pnpm build:firefox",
    "prebuild:chrome": "rm -rf dist/chrome",
    "prebuild:firefox": "rm -rf dist/firefox",
    "build:content": "vite build --config build/vite.content.ts --mode production --outDir dist/chrome && mkdir -p dist/firefox && cp -R dist/chrome/assets dist/chrome/content dist/firefox/ && node build/check-bundle-size.cjs",
    "build:chrome": "pnpm build:content && vite build --config build/vite.chrome.ts --mode production",
    "build:firefox": "pnpm build:content && vite build --config build/vite.firefox.ts --mode production",
    "build:test": "pnpm build && pnpm package:all",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint src tests --ext .ts,.tsx",
    "lint:fix": "eslint src tests --ext .ts,.tsx --fix",
    "format": "prettier --write \"src/**/*.{ts,tsx,css}\" \"tests/**/*.ts\"",
    "format:check": "prettier --check \"src/**/*.{ts,tsx,css}\" \"tests/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "validate": "pnpm generate:pdf-fonts && pnpm lint && pnpm format:check && pnpm typecheck && pnpm test:coverage && pnpm build",
    "package:chrome": "cd dist/chrome && zip -r ../ai-chat-exporter-chrome.zip .",
    "package:firefox": "cd dist/firefox && zip -r ../ai-chat-exporter-firefox.zip .",
    "package:source": "zip -r dist/ai-chat-exporter-source.zip . -x 'node_modules/*' -x 'dist/*' -x '.git/*' -x '*.log' -x '.vscode/*' -x '.idea/*' -x '*.zip' -x 'tmp/*' -x 'coverage/*' -x '.nyc_output/*' -x '.claude/*' -x '.agents/*' -x 'claude-arsenal/*'",
    "package:all": "pnpm build && node build/package-all.cjs",
    "generate:pdf-fonts": "node scripts/generate-pdf-fonts.mjs",
    "prebuild": "pnpm generate:pdf-fonts",
    "postinstall": "pnpm generate:pdf-fonts",
    "probe": "node scripts/selector-probe.mjs"
  },
  "keywords": [
    "browser-extension",
    "chatgpt",
    "ai",
    "export",
    "pdf",
    "markdown"
  ],
  "author": "",
  "license": "MIT",
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.5",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/source-sans-3": "^5.3.0",
    "@types/chrome": "^0.2.2",
    "@types/jsdom": "^27.0.0",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "^4.1.10",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.1.5",
    "jsdom": "^30.0.0",
    "jszip": "^3.10.1",
    "prettier": "^3.5.3",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.65.0",
    "vite": "^8.1.5",
    "vitest": "^4.1.10",
    "wawoff2": "^2.0.1"
  },
  "dependencies": {
    "docx": "^9.7.1",
    "highlight.js": "^11.11.1",
    "jspdf": "^4.2.1",
    "marked": "^17.0.1"
  }
}
